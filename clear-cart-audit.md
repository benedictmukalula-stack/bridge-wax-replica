# Clear Cart Audit

The populated cart drawer now shows a visible, bordered `Clear Cart` button with a trash icon and accessible label `Clear cart` beside the quotation form heading. Live verification confirmed that activating it removes the selected product, returns the header from `Cart 1` to `Cart`, and replaces the drawer contents with `Your basket is empty` and the Continue browsing action.
