# Header Cart Label Audit

The header basket control now displays `Cart` on desktop and mobile. The accessible control text also reads `Open cart`, with the selected-item count retained when products are present. The existing product search field, Contact button, logo, and mobile menu trigger remain in their prior positions. Type checking and production build pass.
